# Copy to secrets.local.yaml and fill in your real values.
# secrets.local.yaml is gitignored and overrides config.yaml.

notify:
  type: discord
  webhook_url: https://discord.com/api/webhooks/XXXXXXXXXX/YYYYYYYYYY
